# CLI Runtime Specification

- **Node.js Environment**: Static HTML + Astro v3 hybrid architecture.
- **Test Runner**: Node test runner (`node --test tests/**/*.test.js`).
- **Build Engine**: Astro build static output (`dist/`).
- **Daemon Preview**: Local preview server at `http://localhost:4321`.
